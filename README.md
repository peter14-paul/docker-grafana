# docker-grafana

### Executar o Container

No terminal, navegue até a pasta onde salvou o arquivo `docker-compose.yaml` e execute:

```bash
docker compose up -d
```

O parâmetro `-d` garante que o serviço rode em segundo plano (*detached mode*).

### Acesse o grafana via web

```
http://localhost:3000
```
